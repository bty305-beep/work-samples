# CLAUDE.md

業務システムと自動化ツールのサンプル集です。
各フォルダは独立して動きます。ビルド工程はなく、`index.html` を開けば動く形を基本にしています。

---

## 意匠の系統（今後の共通基準）

このリポジトリの意匠は、次の4系統を組み合わせた方向に属します。
**意匠の判断に迷ったときは、この系統に照らして決めてください。**

| 系統 | 内容 |
|---|---|
| エディトリアル | 雑誌・カタログのようなレイアウト。写真や文字の組み合わせで見せる |
| タイポグラフィック | 文字のデザインとレイアウトを主役にする |
| ミニマル | 余白を活かし、色数を抑える |
| スイススタイル | 整ったレイアウトとタイポグラフィを重視する |

### 採用しない系統

- **未来・デジタル系**　Y2K、ホログラフィック、ネオン、フューチャリスティック、サイバーパンク、グリッチ
- **グランジ、サイケデリック**
- **ポップ系**　ポップ、マキシマリズム、ポップアート、メンフィス
- **ラグジュアリー**　黒と金の高級感

**理由。** このリポジトリが扱うのは実測データと業務の記録です。
装飾を足すと、内容と見た目が矛盾します。

---

## 統一デザインシステム

上の系統名は、**すでにあるデザインシステムに呼び方を与えたものです。システムの内容は変わりません。**

実体はリポジトリ直下の `index.html` の `:root` にあります。ここが唯一の出所です。

- 色・書体・文字寸法・行間・字間・余白・罫の値は、`:root` の変数をそのまま使う
- **新しいトークンを作らない。** 必要に迫られた場合は、追加ではなく既存の組み合わせで解けないかを先に検討する
- 見出しは明朝（`--font-mincho`）、本文はゴシック（`--font-gothic`）
- 影・角丸・グラデーション・装飾アイコン・絵文字・英語見出しは使わない
- 画面の外に出るもの（PDFなど）の色と書体も、同じ変数から取る。値を二重に持たない
- スマートフォンで指で操作する画面では、ボタンとタップ領域の大きさを十分に確保する。
  これは装飾ではなく操作性の要件で、上の方針と両立させる

---

## 実装の型

- React + Babel Standalone + Tailwind をCDNから読み込む1ファイル完結を基本とする。
  PWAの `manifest.json` とサービスワーカーのように、別ファイルでなければ成立しないものだけを分ける
- Babelは `registerPreset` で classic runtime を指定する
- Tailwindの preflight は実行時に `head` の末尾へ挿入される。
  `body` と `a` は `html body` / `body a` と詳細度を上げて、上書きされないようにする
- 外部の部品はCDNの版を固定する。オフラインで動かす場合は、その版をサービスワーカーで保存する

---

## 記述

- 画面・README・コメントとも日本語。見出しに英語を使わない
- 数値を出すときは出典と取得時点を併記する。架空のデータを載せる場合は、架空である旨を明記する
- できないこと・確認していないことは、そのまま書く
